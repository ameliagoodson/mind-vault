// when user clicks correct or incorrect, it navigates to the next card
// when user clicks either button, it records correct answers in state

export const handleAnswers = ({ filteredFlashcards, isDisabled, setIsDisabled, currentCard,setCurrentCard}) => {

console.log("handle answers clicked");
}
